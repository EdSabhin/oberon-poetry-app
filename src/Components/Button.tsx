import React from "react";

type Props = {};

const Button = (props: Props) => {
  return (
    <button className="border-none py-2 px-6 mt-4 font-bold bg-indigo-700 rounded italic">
      Read
    </button>
  );
};

export default Button;
